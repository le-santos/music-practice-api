<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= "Music Practice Journal" %></title>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

  <%= stylesheet_link_tag 'application', media: 'all' %>
</head>
<body class="bg-gray-50">
  <%= render 'web/shared/navbar' %>

  <div class="px-4 py-4">
    <% if notice.present? %>
      <div class="mb-4 rounded-md bg-green-50 border border-green-200 p-4" role="alert">
        <p class="text-sm font-medium text-green-800"><%= notice %></p>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="mb-4 rounded-md bg-red-50 border border-red-200 p-4" role="alert">
        <p class="text-sm font-medium text-red-800"><%= alert %></p>
      </div>
    <% end %>
    <% if flash[:error].present? %>
      <div class="mb-4 rounded-md bg-red-50 border border-red-200 p-4" role="alert">
        <p class="text-sm font-medium text-red-800"><%= flash[:error] %></p>
      </div>
    <% end %>
  </div>

  <main class="max-w-6xl mx-auto px-4 py-8">
    <%= yield %>
  </main>

  <footer class="bg-white border-t border-gray-200 py-6 mt-12">
    <div class="max-w-6xl mx-auto px-4 text-center text-gray-600 text-sm">
      <p>&copy; <%= Time.current.year %> Music Practice Journal </p>
    </div>
  </footer>
</body>
</html>
